import 'package:flutter/material.dart';

class tesisInformation extends StatelessWidget {
  const tesisInformation({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Perro"));
  }
}
